import "@/global.css"

import { Icons } from "@/components/icons"
import {
  Nav,
  NavCategory,
  NavContent,
  NavFooter,
  NavHeader,
  NavLink,
  NavProfile,
  NavSeperator,
} from "@/components/nav"

import { TooltipProvider } from "@/components/ui/tooltip"

export default function App({ Component, pageProps }) {
  return (
    <TooltipProvider>
      <div className="fixed flex h-screen w-screen">
        <Nav>
          <NavHeader>
            <Icons.Logo className="h-8 w-8 shrink-0 fill-primary" />
            <p className="font-bold">Easy Intake</p>
          </NavHeader>
          <NavContent>
            <NavCategory title="Cats" icon={Icons.Edit}>
              <NavLink
                href="/"
                icon={Icons.Mic}
                label="Summarize"
                notifications={1}
              />
              <NavLink
                href="/test"
                icon={Icons.Trash}
                label="Test"
                notifications={99}
              />
            </NavCategory>
          </NavContent>
          <NavFooter>
            <NavLink href="/settings" icon={Icons.Settings} label="Settings" />
            <NavProfile />
          </NavFooter>
        </Nav>
        <main className="w-full overflow-y-scroll">
          <Component {...pageProps} />
        </main>
      </div>
    </TooltipProvider>
  )
}
