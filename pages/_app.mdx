import "@/global.css"

import { Icons } from "@/components/icons"
import {
  Nav,
  NavCategory,
  NavContent,
  NavFooter,
  NavHeader,
  NavLink,
  NavProfile,
  NavSeperator,
} from "@/components/nav"

import { TooltipProvider } from "@/components/ui/tooltip"

export default function App({ Component, pageProps }) {
  return (
    <TooltipProvider>
      <div className="fixed flex h-screen w-screen">
        <Nav>
          <NavHeader>
            <Icons.Logo className="w-6 h-6 shrink-0 fill-primary" />
            <p className="font-bold">Easy Intake</p>
          </NavHeader>
          <NavContent>
            <NavCategory title="Cats" icon={Icons.Edit}>
              <NavLink href="/a" icon={Icons.Mic} label="A" notifications={1} />
              <NavLink
                href="/b"
                icon={Icons.Trash}
                label="B"
                notifications={99}
              />
            </NavCategory>
            <NavCategory title="Cats2" icon={Icons.Edit}>
              <NavLink href="/c" icon={Icons.Mic} label="C" notifications={1} />
              <NavLink
                href="/d"
                icon={Icons.Trash}
                label="D"
                notifications={99}
              />
            </NavCategory>
          </NavContent>
          <NavFooter>
            <NavLink href="/settings" icon={Icons.Settings} label="Settings" />
            <NavProfile />
          </NavFooter>
        </Nav>
        <main className="w-full overflow-y-scroll">
          <Component {...pageProps} />
        </main>
      </div>
    </TooltipProvider>
  )
}
