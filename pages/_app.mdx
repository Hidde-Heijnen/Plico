import "@/global.css"

import { Icons } from "@/components/icons"
import {
  Nav,
  NavContent,
  NavExtendable,
  NavFooter,
  NavHeader,
  NavLink,
  NavProfile,
  NavSeperator,
} from "@/components/nav"

import { TooltipProvider } from "@/components/ui/tooltip"

import { ScrollArea } from "@/components/ui/scroll-area"

export default function App({ Component, pageProps }) {
  return (
    <TooltipProvider>
      <div className="flex h-screen w-screen">
        <Nav size={10}>
          <NavHeader>
            <Icons.Logo className="h-6 w-6 shrink-0 fill-primary" />
            <p className="font-bold">Plico</p>
          </NavHeader>
          <NavContent>
            <NavLink
              href="/"
              icon={Icons.Home}
              label="Home"
              notifications={1}
            />
            <NavLink
              href="/docs"
              icon={Icons.Docs}
              label="Docs"
              notifications={99}
            />
            <NavExtendable title="Cats" icon={Icons.Edit}>
              <NavLink href="/a" icon={Icons.Mic} label="A" notifications={1} />
              <NavLink
                href="/b"
                icon={Icons.Trash}
                label="B"
              />
              <NavExtendable title="Cats2" icon={Icons.Mic}>
                <NavLink
                  href="/c"
                  icon={Icons.Mic}
                  label="C"
                />
                <NavLink
                  href="/d"
                  icon={Icons.Trash}
                  label="D"
                  notifications={2}

                />
              </NavExtendable>
            </NavExtendable>
          </NavContent>
          <NavFooter>
            <NavLink href="/settings" icon={Icons.Settings} label="Settings" />
            <NavProfile />
          </NavFooter>
        </Nav>
        <ScrollArea className="w-full">
          <main >
            <Component {...pageProps} />
          </main>
        </ScrollArea>
      </div>
    </TooltipProvider>

)
}
