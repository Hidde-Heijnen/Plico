import "@/global.css"

import { Icons } from "@/components/icons"
import {
  Nav,
  NavCategory,
  NavCollapsableItem,
  NavContent,
  NavFooter,
  NavHeader,
  NavLink,
  NavProfile,
  NavSeperator,
} from "@/components/nav"

import { TooltipProvider } from "@/components/ui/tooltip"

import { ScrollArea } from "@/components/ui/scroll-area"

export default function App({ Component, pageProps }) {
  return (
    <TooltipProvider>
      <div className="flex h-screen w-screen">
        <Nav size={10}>
          <NavHeader>
            <Icons.Logo className="h-6 w-6 shrink-0 fill-primary" />
            <p className="font-bold">Plico</p>
          </NavHeader>
          <NavContent>
            <NavCategory label="blalaal" links={[
              {
                href:"/",
                icon:Icons.Home,
                label:"Home",
                notifications:1
              },
              {
                href:"/docs",
                icon:Icons.Docs,
                label:"Docs",
                notifications:100
              }
            ]}/>
            <NavCategory label="blalaalasdfasdfasfdasdfadsfasdadf" links={[
              {
                href:"/bla",
                icon:Icons.Home,
                label:"Hom1",
                notifications:1
              },
              {
                href:"/bla2",
                icon:Icons.Docs,
                label:"Docs2",
                notifications:100,
                links:[
                  {
                    href:"/bla3",
                    icon:Icons.Home,
                    label:"Home3",
                    notifications:6

                  },
                  {
                    href:"/bla4",
                    icon:Icons.Docs,
                    label:"Docs4",
                    links: [
                      {
                        href: "/bla5",
                        icon: Icons.Home,
                        label: "Home5",
                        notifications: 0
                      }
                    ]
                  }
                ]
              }
            ]}/>
          </NavContent>
          <NavFooter>
            <NavLink href="/settings" icon={Icons.Settings} label="Settings" />
            <NavProfile />
          </NavFooter>
        </Nav>
        <ScrollArea className="w-full">
          <main >
            <Component {...pageProps} />
          </main>
        </ScrollArea>
      </div>
    </TooltipProvider>

)
}
